import { Routes } from '@angular/router';
import { DashboardComponent } from './features/weather/components/dashboard/dashboard.component';
export const routes: Routes = [
    {
        path: '',
        component: DashboardComponent
    }
];
